package List_컬렉션;

import java.awt.List;

// List컬렉션
// List컬렉션 기본문법

public class List_Collection {
	/*
	List<String> list = ```;
	list.add("홍길동");				//맨끝에 객체 추가
	list.add(1, "신용권");			//지정된 인덱스에 객체 삽입
	String str = list.getItem(1);	//인덱스로 객체 찾기
	list.remove(0);					//인덱스로 객체 삭제
	list.remove("신용권");			//객체 삭제
	
	List<String> list = ```;
	for(int i = 0; i<list.size(); i++){
		String str = list.get(i);	//저장된 총 객체 수만큼 루핑
	}
	
	for(String str : list) {	//저장된 총 객체 수만큼 루핑
	}
	*/
}
